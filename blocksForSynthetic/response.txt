Here is an ANPL code which implements your requirements:

```python
def process_string(input_str):
    input_str = `Remove all exclamation points, question marks, and periods from the string`(input_str)
    input_str = `Make every other character in the string uppercase`(input_str)
    input_str = `Remove all instances of the letter 'e' from the string`(input_str)
    input_str = `Reverse the order of words in the string`(input_str)
    input_str = `Remove the first and last two characters of the string`(input_str),
    output_str = `Drop the last half of the string, as computed by characters`(input_str)
    return output_str
```

Each of the quotations inside the backticks (`...`) corresponds to a natural language hole, which represents the natural language description of what needs to be done. During the compiling process, these holes will be filled using LLMs (Language Learning Models).